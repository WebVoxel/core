/**
 * Raw block data. Used when instantiating a block or importing block data.
 * @author RailRunner16
 */
export interface IBlockData {
    type: string;
    x: number;
    y: number;
    z: number;
}